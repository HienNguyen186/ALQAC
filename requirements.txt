# ── Core retrieval ────────────────────────────────────────────────────
rank-bm25>=0.2.2
numpy>=1.26.0
tqdm>=4.66.0
pyyaml>=6.0.2
ftfy>=6.2.0
requests>=2.32.0
python-dotenv>=1.0.1

# ── BGE-M3 hybrid dense+sparse retrieval ─────────────────────────────
# FlagEmbedding cung cấp BGEM3FlagModel với đầy đủ dense / sparse / colbert
FlagEmbedding>=1.3.0
# sentence-transformers vẫn giữ để fallback và compatibility
sentence-transformers>=3.0.1

# ── Local LLM inference ───────────────────────────────────────────────
torch>=2.3.0
transformers>=4.44.0
accelerate>=0.33.0
bitsandbytes>=0.43.0; platform_system != "Windows"

# ── Evaluation & testing ─────────────────────────────────────────────
scikit-learn>=1.5.0
pandas>=2.2.0
pytest>=8.2.0

# ── Notebook / Colab convenience ──────────────────────────────────────
jupyter>=1.0.0
